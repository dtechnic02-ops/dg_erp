@extends('admin.layout')

@section('title', 'Platform Settings')

@section('content')
@php
    $socialLinks = $setting->socialLinks->keyBy('provider');
    $providers = ['facebook', 'instagram', 'x', 'linkedin', 'youtube', 'tiktok', 'telegram'];
    $gatewayLabels = ['esewa' => 'eSewa', 'khalti' => 'Khalti', 'fonepay' => 'Fonepay', 'stripe' => 'Stripe', 'paypal' => 'PayPal', 'razorpay' => 'Razorpay'];
@endphp
<div class="dg-page">
    <header class="dg-page-header"><div class="dg-page-header-content"><h2 class="dg-page-title">Platform Settings</h2><p class="dg-page-subtitle">Secure platform-wide configuration. Changes do not alter existing company records or payment workflows.</p></div></header>
    @if(session('success'))<div class="alert alert-success dg-alert" role="alert">{{ session('success') }}</div>@endif
    @if(session('error'))<div class="alert alert-danger dg-alert" role="alert">{{ session('error') }}</div>@endif
    @if($errors->any())<div class="alert alert-danger dg-alert" role="alert">Please correct the highlighted fields and submit again.</div>@endif

    <nav class="nav nav-pills dg-tabs mb-3" aria-label="Platform settings sections">
        <a class="nav-link" href="#general">General</a><a class="nav-link" href="#branding">Branding</a><a class="nav-link" href="#social">Social Media</a><a class="nav-link" href="#smtp">SMTP</a><a class="nav-link" href="#gateways">Payment Gateways</a>
    </nav>

    <section id="general" class="dg-section" aria-labelledby="general-title"><div class="dg-card card"><div class="dg-card-header card-header"><h3 id="general-title" class="h6 mb-0">General, contact, localization and defaults</h3></div><div class="dg-card-body card-body">
        <form method="POST" action="{{ route('admin.platform-settings.general.update') }}" class="dg-form">@csrf @method('PUT')
            <div class="row g-3"><div class="col-md-6"><label for="platform_name" class="form-label">Platform name</label><input id="platform_name" name="platform_name" class="form-control dg-input @error('platform_name') is-invalid @enderror" value="{{ old('platform_name', $setting->platform_name) }}" required>@error('platform_name')<div class="invalid-feedback">{{ $message }}</div>@enderror</div><div class="col-md-6"><label for="legal_company_name" class="form-label">Legal company name</label><input id="legal_company_name" name="legal_company_name" class="form-control dg-input" value="{{ old('legal_company_name', $setting->legal_company_name) }}"></div>
                <div class="col-md-6"><label for="owner_name" class="form-label">Owner name</label><input id="owner_name" name="owner_name" class="form-control dg-input @error('owner_name') is-invalid @enderror" value="{{ old('owner_name', $setting->owner_name) }}" required>@error('owner_name')<div class="invalid-feedback">{{ $message }}</div>@enderror</div><div class="col-md-6"><label for="website_url" class="form-label">Website</label><input id="website_url" type="url" name="website_url" class="form-control dg-input @error('website_url') is-invalid @enderror" value="{{ old('website_url', $setting->website_url) }}" autocomplete="url">@error('website_url')<div class="invalid-feedback">{{ $message }}</div>@enderror</div>
                <div class="col-md-6"><label for="primary_email" class="form-label">Primary email</label><input id="primary_email" type="email" name="primary_email" class="form-control dg-input @error('primary_email') is-invalid @enderror" value="{{ old('primary_email', $setting->primary_email) }}" required>@error('primary_email')<div class="invalid-feedback">{{ $message }}</div>@enderror</div><div class="col-md-6"><label for="primary_mobile" class="form-label">Primary mobile</label><input id="primary_mobile" name="primary_mobile" class="form-control dg-input" value="{{ old('primary_mobile', $setting->primary_mobile) }}" required></div>
                <div class="col-md-4"><label for="alternate_mobile" class="form-label">Alternate mobile</label><input id="alternate_mobile" name="alternate_mobile" class="form-control dg-input" value="{{ old('alternate_mobile', $setting->alternate_mobile) }}"></div><div class="col-md-4"><label for="support_email" class="form-label">Support email</label><input id="support_email" type="email" name="support_email" class="form-control dg-input" value="{{ old('support_email', $setting->support_email) }}"></div><div class="col-md-4"><label for="support_mobile" class="form-label">Support mobile</label><input id="support_mobile" name="support_mobile" class="form-control dg-input" value="{{ old('support_mobile', $setting->support_mobile) }}"></div>
                <div class="col-md-4"><label for="whatsapp_number" class="form-label">WhatsApp number</label><input id="whatsapp_number" name="whatsapp_number" class="form-control dg-input" value="{{ old('whatsapp_number', $setting->whatsapp_number) }}"></div><div class="col-md-4"><label for="country" class="form-label">Country</label><input id="country" name="country" class="form-control dg-input" value="{{ old('country', $setting->country) }}"></div><div class="col-md-4"><label for="state_province" class="form-label">State / Province</label><input id="state_province" name="state_province" class="form-control dg-input" value="{{ old('state_province', $setting->state_province) }}"></div>
                <div class="col-md-4"><label for="district_city" class="form-label">District / City</label><input id="district_city" name="district_city" class="form-control dg-input" value="{{ old('district_city', $setting->district_city) }}"></div><div class="col-md-4"><label for="municipality" class="form-label">Municipality</label><input id="municipality" name="municipality" class="form-control dg-input" value="{{ old('municipality', $setting->municipality) }}"></div><div class="col-md-2"><label for="ward_number" class="form-label">Ward</label><input id="ward_number" name="ward_number" class="form-control dg-input" value="{{ old('ward_number', $setting->ward_number) }}"></div><div class="col-md-2"><label for="postal_code" class="form-label">Postal code</label><input id="postal_code" name="postal_code" class="form-control dg-input" value="{{ old('postal_code', $setting->postal_code) }}"></div>
                <div class="col-12"><label for="full_address" class="form-label">Full address</label><textarea id="full_address" name="full_address" class="form-control dg-textarea" rows="2">{{ old('full_address', $setting->full_address) }}</textarea></div>
                <div class="col-md-6"><label for="tax_number" class="form-label">PAN / Tax number</label><input id="tax_number" name="tax_number" class="form-control dg-input" value="{{ old('tax_number', $setting->tax_number) }}"></div><div class="col-md-6"><label for="vat_number" class="form-label">VAT number</label><input id="vat_number" name="vat_number" class="form-control dg-input" value="{{ old('vat_number', $setting->vat_number) }}"></div><div class="col-md-6"><label for="company_registration_number" class="form-label">Company registration number</label><input id="company_registration_number" name="company_registration_number" class="form-control dg-input" value="{{ old('company_registration_number', $setting->company_registration_number) }}"></div><div class="col-md-6"><label for="business_license_number" class="form-label">Business license number</label><input id="business_license_number" name="business_license_number" class="form-control dg-input" value="{{ old('business_license_number', $setting->business_license_number) }}"></div>
                <div class="col-md-3"><label for="timezone" class="form-label">Timezone</label><input id="timezone" name="timezone" class="form-control dg-input @error('timezone') is-invalid @enderror" value="{{ old('timezone', $setting->timezone) }}" required>@error('timezone')<div class="invalid-feedback">{{ $message }}</div>@enderror</div><div class="col-md-2"><label for="currency_code" class="form-label">Currency</label><input id="currency_code" name="currency_code" class="form-control dg-input" value="{{ old('currency_code', $setting->currency_code) }}" required></div><div class="col-md-2"><label for="language_code" class="form-label">Language</label><input id="language_code" name="language_code" class="form-control dg-input" value="{{ old('language_code', $setting->language_code) }}" required></div><div class="col-md-2"><label for="date_format" class="form-label">Date format</label><input id="date_format" name="date_format" class="form-control dg-input" value="{{ old('date_format', $setting->date_format) }}" required></div><div class="col-md-3"><label for="time_format" class="form-label">Time format</label><input id="time_format" name="time_format" class="form-control dg-input" value="{{ old('time_format', $setting->time_format) }}" required></div>
                <div class="col-md-3"><label for="default_trial_days" class="form-label">Default trial days</label><input id="default_trial_days" type="number" min="0" name="default_trial_days" class="form-control dg-input" value="{{ old('default_trial_days', $setting->default_trial_days) }}" required></div><div class="col-md-3"><label for="default_staff_limit" class="form-label">Default staff limit</label><input id="default_staff_limit" type="number" min="0" name="default_staff_limit" class="form-control dg-input" value="{{ old('default_staff_limit', $setting->default_staff_limit) }}"></div><div class="col-md-3"><label for="default_customer_limit" class="form-label">Default customer limit</label><input id="default_customer_limit" type="number" min="0" name="default_customer_limit" class="form-control dg-input" value="{{ old('default_customer_limit', $setting->default_customer_limit) }}"></div><div class="col-md-3"><label for="default_product_limit" class="form-label">Default product limit</label><input id="default_product_limit" type="number" min="0" name="default_product_limit" class="form-control dg-input" value="{{ old('default_product_limit', $setting->default_product_limit) }}"></div>
            </div><div class="mt-3"><button class="btn btn-primary dg-btn dg-btn-primary" type="submit">Save platform settings</button></div>
        </form>
    </div></div></section>

    <section id="branding" class="dg-section" aria-labelledby="branding-title"><div class="dg-card card"><div class="dg-card-header card-header"><h3 id="branding-title" class="h6 mb-0">Branding assets</h3></div><div class="dg-card-body card-body"><form method="POST" action="{{ route('admin.platform-settings.branding.update') }}" enctype="multipart/form-data" class="dg-form">@csrf @method('PUT')<div class="row g-3">
        @foreach(['logo' => ['Logo', $setting->logo_path, 'jpg,jpeg,png,webp'], 'favicon' => ['Favicon', $setting->favicon_path, 'png,ico'], 'signature' => ['Signature', $setting->signature_path, 'jpg,jpeg,png,webp'], 'profile_photo' => ['Owner / profile photo', $setting->profile_photo_path, 'jpg,jpeg,png,webp']] as $field => [$label, $path, $types])
            <div class="col-md-6">
                <label for="{{ $field }}" class="form-label">{{ $label }}</label>
                <input id="{{ $field }}" type="file" name="{{ $field }}" class="form-control dg-input {{ $errors->has($field) ? 'is-invalid' : '' }}" accept="image/{{ str_replace('jpg,jpeg,', 'jpeg,', $types) }}">
                @if($errors->has($field))<div class="invalid-feedback">{{ $errors->first($field) }}</div>@endif
                @if($path)<div class="dg-upload-preview mt-2"><img src="{{ asset('storage/'.$path) }}" alt="Current {{ strtolower($label) }}"><span>Current file stored</span></div>@endif
            </div>
        @endforeach
    </div><p class="form-text">Only approved image formats are accepted. Existing files remain unchanged when no replacement is selected.</p><button class="btn btn-primary dg-btn dg-btn-primary" type="submit">Update branding</button></form></div></div></section>

    <section id="social" class="dg-section" aria-labelledby="social-title"><div class="dg-card card"><div class="dg-card-header card-header"><h3 id="social-title" class="h6 mb-0">Social media links</h3></div><div class="dg-card-body card-body"><form method="POST" action="{{ route('admin.platform-settings.social.update') }}" class="dg-form">@csrf @method('PUT')<div class="dg-table-scroll"><table class="table dg-table"><thead class="dg-head"><tr><th>Provider</th><th>URL</th><th>Active</th><th>Order</th><th>Remove</th></tr></thead><tbody>
        @foreach($providers as $index => $provider) @php($link = $socialLinks->get($provider)) <tr class="dg-provider-row"><td><input name="links[{{ $index }}][provider]" class="form-control dg-input" value="{{ old("links.$index.provider", $provider) }}" aria-label="Provider"></td><td><input type="url" name="links[{{ $index }}][url]" class="form-control dg-input" value="{{ old("links.$index.url", $link?->url) }}" aria-label="URL"></td><td><input type="hidden" name="links[{{ $index }}][is_active]" value="0"><input type="checkbox" class="form-check-input" name="links[{{ $index }}][is_active]" value="1" {{ old("links.$index.is_active", $link?->is_active) ? 'checked' : '' }} aria-label="Active"></td><td><input type="number" min="0" name="links[{{ $index }}][display_order]" class="form-control dg-input" value="{{ old("links.$index.display_order", $link?->display_order ?? $index) }}" aria-label="Display order"></td><td><input type="checkbox" class="form-check-input" name="links[{{ $index }}][remove]" value="1" aria-label="Remove"></td></tr> @endforeach
    </tbody></table></div><p class="form-text">Leave URL empty to omit a provider. Provider names are stored as reusable records, not fixed database columns.</p><button class="btn btn-primary dg-btn dg-btn-primary" type="submit">Save social links</button></form></div></div></section>

    <section id="smtp" class="dg-section" aria-labelledby="smtp-title"><div class="dg-card card"><div class="dg-card-header card-header"><h3 id="smtp-title" class="h6 mb-0">SMTP configuration</h3></div><div class="dg-card-body card-body"><form method="POST" action="{{ route('admin.platform-settings.smtp.update') }}" class="dg-form">@csrf @method('PUT')<div class="row g-3"><div class="col-md-3"><label for="mailer" class="form-label">Mailer</label><select id="mailer" name="mailer" class="form-select dg-select"><option value="smtp" selected>SMTP</option></select></div><div class="col-md-5"><label for="host" class="form-label">SMTP host</label><input id="host" name="host" class="form-control dg-input" value="{{ old('host', $setting->smtpSetting?->host) }}" required></div><div class="col-md-2"><label for="port" class="form-label">Port</label><input id="port" type="number" name="port" min="1" max="65535" class="form-control dg-input" value="{{ old('port', $setting->smtpSetting?->port) }}" required></div><div class="col-md-2"><label for="encryption" class="form-label">Encryption</label><select id="encryption" name="encryption" class="form-select dg-select"><option value="">None</option>@foreach(['tls','ssl','starttls'] as $value)<option value="{{ $value }}" {{ old('encryption', $setting->smtpSetting?->encryption) === $value ? 'selected' : '' }}>{{ strtoupper($value) }}</option>@endforeach</select></div><div class="col-md-6"><label for="smtp_username" class="form-label">Username</label><input id="smtp_username" name="username" class="form-control dg-input" value="{{ old('username', $setting->smtpSetting?->username) }}"></div><div class="col-md-6"><label for="smtp_password" class="form-label">New password</label><input id="smtp_password" type="password" name="password" class="form-control dg-input" autocomplete="new-password"><div class="dg-secret-status">{{ $setting->smtpSetting?->getRawOriginal('password') ? 'Password is configured' : 'Password is not configured' }}</div></div><div class="col-md-6"><label for="from_address" class="form-label">From email</label><input id="from_address" type="email" name="from_address" class="form-control dg-input" value="{{ old('from_address', $setting->smtpSetting?->from_address) }}" required></div><div class="col-md-6"><label for="from_name" class="form-label">From name</label><input id="from_name" name="from_name" class="form-control dg-input" value="{{ old('from_name', $setting->smtpSetting?->from_name ?? $setting->platform_name) }}" required></div><div class="col-12"><input type="hidden" name="is_active" value="0"><div class="form-check"><input id="smtp_active" type="checkbox" name="is_active" value="1" class="form-check-input" {{ old('is_active', $setting->smtpSetting?->is_active) ? 'checked' : '' }}><label for="smtp_active" class="form-check-label">Enable stored SMTP configuration</label></div></div></div><div class="mt-3 d-flex gap-2 flex-wrap"><button class="btn btn-primary dg-btn dg-btn-primary" type="submit">Save SMTP configuration</button></form><form method="POST" action="{{ route('admin.platform-settings.smtp.test') }}">@csrf<button class="btn btn-outline-secondary dg-btn dg-btn-light" type="submit">Test to my Super Admin email</button></form></div></div></div></section>

    <section id="gateways" class="dg-section" aria-labelledby="gateways-title"><div class="dg-section-header"><div><h3 id="gateways-title" class="dg-section-title">Payment gateways</h3><p class="dg-section-subtitle">Configuration storage only. No payment processing or webhook route is enabled here.</p></div></div><div class="row g-3">
        @foreach($gatewayLabels as $gateway => $label) @php($configured = $gateways->get($gateway)) <div class="col-lg-6"><div class="dg-card card dg-gateway-card"><div class="dg-card-header card-header"><strong>{{ $label }}</strong></div><div class="dg-card-body card-body"><form method="POST" action="{{ route('admin.platform-settings.gateway.update') }}" class="dg-form">@csrf @method('PUT')<input type="hidden" name="gateway" value="{{ $gateway }}"><div class="row g-2"><div class="col-md-6"><label for="display_name_{{ $gateway }}" class="form-label">Display name</label><input id="display_name_{{ $gateway }}" name="display_name" class="form-control dg-input" value="{{ old('display_name', $configured?->display_name ?? $label) }}" required></div><div class="col-md-6"><label for="environment_{{ $gateway }}" class="form-label">Environment</label><select id="environment_{{ $gateway }}" name="environment" class="form-select dg-select"><option value="sandbox" {{ old('environment', $configured?->environment) === 'sandbox' ? 'selected' : '' }}>Sandbox</option><option value="live" {{ old('environment', $configured?->environment) === 'live' ? 'selected' : '' }}>Live</option></select></div><div class="col-md-6"><label for="merchant_id_{{ $gateway }}" class="form-label">Merchant ID</label><input id="merchant_id_{{ $gateway }}" name="merchant_id" class="form-control dg-input" value="{{ old('merchant_id', $configured?->merchant_id) }}"></div><div class="col-md-6"><label for="public_key_{{ $gateway }}" class="form-label">Public key</label><input id="public_key_{{ $gateway }}" name="public_key" class="form-control dg-input" value="{{ old('public_key', $configured?->public_key) }}"></div><div class="col-md-6"><label for="secret_key_{{ $gateway }}" class="form-label">New secret key</label><input id="secret_key_{{ $gateway }}" type="password" name="secret_key" class="form-control dg-input" autocomplete="new-password"><div class="dg-secret-status">{{ $configured?->getRawOriginal('secret_key') ? 'Secret key is configured' : 'Secret key is not configured' }}</div></div><div class="col-md-6"><label for="webhook_secret_{{ $gateway }}" class="form-label">New webhook secret</label><input id="webhook_secret_{{ $gateway }}" type="password" name="webhook_secret" class="form-control dg-input" autocomplete="new-password"><div class="dg-secret-status">{{ $configured?->getRawOriginal('webhook_secret') ? 'Webhook secret is configured' : 'Webhook secret is not configured' }}</div></div><div class="col-12"><input type="hidden" name="is_active" value="0"><div class="form-check"><input id="active_{{ $gateway }}" type="checkbox" name="is_active" value="1" class="form-check-input" {{ old('is_active', $configured?->is_active) ? 'checked' : '' }}><label for="active_{{ $gateway }}" class="form-check-label">Enable this gateway</label></div><p class="form-text text-warning mb-0">Live mode is stored only; it does not activate payment processing.</p></div></div><button class="btn btn-primary dg-btn dg-btn-primary mt-3" type="submit">Save {{ $label }}</button></form></div></div></div> @endforeach
    </div></section>
</div>
@endsection
